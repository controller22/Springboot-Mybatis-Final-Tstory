<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="shop.mtcoding.tstory.model.user.UserRepository">

	<select id="findById"
		resultType="shop.mtcoding.tstory.model.user.User">
		SELECT * FROM user WHERE user_id=#{userId}
	</select>

	<select id="findAll"
		resultType="shop.mtcoding.tstory.model.user.User">
		SELECT *
		FROM user 
		WHERE user_id=#{userId}
	</select>

	<insert id="insert">
		INSERT
		INTO user(username, password, email,profile_img, updated_at, created_at)
		VALUES(#{username}, #{password}, #{email}, #{profileImg}, now(), now())
	</insert>

    <update id="update">
          UPDATE user
		  SET username = #{username},
		  password = #{password},
		  email = #{email},
		  profile_img = #{profileImg}

    </update>

	<delete id="delete">
		DELETE FROM user WHERE user_id = #{userId}
	</delete>



</mapper>